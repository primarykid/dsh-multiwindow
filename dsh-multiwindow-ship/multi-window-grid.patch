diff --git a/packages/client/runtime/src/client/contract/sessions.ts b/packages/client/runtime/src/client/contract/sessions.ts
index 27b9d2d..9d28c48 100644
--- a/packages/client/runtime/src/client/contract/sessions.ts
+++ b/packages/client/runtime/src/client/contract/sessions.ts
@@ -11,7 +11,7 @@ import type { Context } from '@deepseek-ai/cordis'
 import type {
   RpcResult, SessionId, SubagentAddress,
 } from '@deepseek-ai/dsh-api-remotes/client'
-import type { HostObservable, SessionMaybeProvideInfo } from '@deepseek-ai/dsh-client-ui-slots'
+import type { HostObservable, SessionMaybeProvideInfo, SessionProvideInfo } from '@deepseek-ai/dsh-client-ui-slots'
 import type { AgentContext } from '../agents/scope.ts'
 import type { SessionSearchResultItem } from '../sessions/manager.ts'
 import type {
@@ -28,6 +28,13 @@ export interface ISessions {
   readonly list: ObservableSnapshot<SessionListState>
   /** Atomic current-session provide projection (the renderer host's `sessions.provideInfo` feed). */
   readonly currentProvideInfo: HostObservable<SessionMaybeProvideInfo>
+  /**
+   * Resolve one session's render-layer standard-props bundle as an observable
+   * (multi-pane seat). Mint the scope on first resolve; listed sessions only.
+   * @param id - session id.
+   * @returns an observable of the bundle, `undefined` while the session cannot resolve.
+   */
+  infoFor(id: SessionId): HostObservable<SessionProvideInfo | undefined>
   /**
    * The `session.search` result bound the wire schema fixes, exposed to
    * presentation as injected data. Not per-connection state: every transport
@@ -39,6 +46,12 @@ export interface ISessions {
    * @param id - session id (must exist in the list; unknown ids fail loud).
    */
   open(id: SessionId): void
+  /**
+   * Open a pinned session's history window without making it the current
+   * selection (the multi-pane grid's per-pane feed). Idempotent.
+   * @param id - session id.
+   */
+  pinOpen(id: SessionId): void
   /**
    * Open a healthy catalog child through its exact direct-parent address.
    * @param address - catalog-derived parent and child ids.
diff --git a/packages/client/runtime/src/client/sessions/provide.ts b/packages/client/runtime/src/client/sessions/provide.ts
index 1ff3f1e..4fae159 100644
--- a/packages/client/runtime/src/client/sessions/provide.ts
+++ b/packages/client/runtime/src/client/sessions/provide.ts
@@ -34,6 +34,8 @@ export class SessionProvideChannel {
   private currentSnapshot: SessionMaybeProvideInfo
   /** Projection subscribers (plain cell: bundles hold live session sources, so no store freeze may touch them). */
   private readonly listeners = new Set<() => void>()
+  /** Pinned-session projection subscribers — fired on every roster change so multi-pane bundles rebuild. */
+  private readonly pinnedListeners = new Set<() => void>()
 
   /**
    * Atomic current-session provide projection: selection changes and
@@ -96,6 +98,18 @@ export class SessionProvideChannel {
     }
   }
 
+  /**
+   * Subscribe to provider-roster changes. Pinned-session bundles rebuild on
+   * every roster change (the same edge that republishes the current bundle),
+   * so a multi-pane consumer re-resolves on this signal.
+   * @param fn - roster-change callback.
+   * @returns unsubscriber.
+   */
+  onRosterChange(fn: () => void): () => void {
+    this.pinnedListeners.add(fn)
+    return () => { this.pinnedListeners.delete(fn) }
+  }
+
   /**
    * Re-derive the current selection's bundle and publish it when it changed.
    * Bundles are identity-stable per (scope, roster) materialization, so an
@@ -169,6 +183,13 @@ export class SessionProvideChannel {
     this.maybeInfoCache = this.materializeMaybeInfo()
     this.host.rebuildBundles()
     this.publishCurrent()
+    for (const fn of [...this.pinnedListeners]) {
+      try {
+        fn()
+      } catch (error) {
+        console.error('sessions.pinnedInfo subscriber failed:', error)
+      }
+    }
   }
 
   /** Build the static no-session kit and reject duplicate declared names. */
diff --git a/packages/client/runtime/src/client/sessions/service.ts b/packages/client/runtime/src/client/sessions/service.ts
index c66da4e..b8540af 100644
--- a/packages/client/runtime/src/client/sessions/service.ts
+++ b/packages/client/runtime/src/client/sessions/service.ts
@@ -259,6 +259,8 @@ export class SessionRuntime implements ISessions {
   private readonly scopes = new Map<SessionId, ScopeRecord>()
   /** The provide channel (roster, materialization rules, current projection) — shared with the test runtime's double. */
   private readonly provideChannel: SessionProvideChannel
+  /** Per-session pinned provide observables (multi-pane seat), cached by id. */
+  private readonly pinnedInfoCache = new Map<SessionId, HostObservable<SessionProvideInfo | undefined>>()
   /**
    * The staged session id — follows `list.current` exactly, holding its last
    * defined value across masked gaps (a transiently absent selection blanks
@@ -365,6 +367,35 @@ export class SessionRuntime implements ISessions {
     return this.provideChannel.provide(descriptor)
   }
 
+  /**
+   * Resolve one session's render-layer standard-props bundle as an observable
+   * (multi-pane seat). Unlike {@link SessionRuntime.currentProvideInfo} this
+   * keys off an explicit session id rather than the current selection,
+   * minting the scope on first resolve (listed sessions only; unknown ids
+   * snapshot `undefined`). The bundle re-resolves when the provider roster
+   * changes or the session's list row moves.
+   * @param id - session id.
+   * @returns an observable of the bundle, `undefined` while the session cannot resolve.
+   */
+  infoFor(id: SessionId): HostObservable<SessionProvideInfo | undefined> {
+    let obs = this.pinnedInfoCache.get(id)
+    if (obs === undefined) {
+      obs = {
+        getSnapshot: () => this.provideInfo(id),
+        subscribe: (fn) => {
+          const offRoster = this.provideChannel.onRosterChange(() => fn())
+          const offList = this.list.subscribe(() => fn())
+          return () => {
+            offRoster()
+            offList()
+          }
+        },
+      }
+      this.pinnedInfoCache.set(id, obs)
+    }
+    return obs
+  }
+
   /**
    * Select a listed or retained catalog-addressed session as current.
    * @param id - listed or addressed session id.
@@ -373,6 +404,23 @@ export class SessionRuntime implements ISessions {
     this.manager.select(id)
   }
 
+  /**
+   * Open a pinned session's history window without making it the current
+   * selection (the multi-pane grid's per-pane feed). Resolves the session
+   * scope and pulls its latest page like an uncontrolled open; idempotent —
+   * {@link Session.open} reuses an in-flight/all-open window. No-ops for an
+   * unlisted or already-open session.
+   * @param id - session id.
+   */
+  pinOpen(id: SessionId): void {
+    const record = this.resolve(id)
+    // The pane list gates on the session being listed (byId), so resolve
+    // cannot miss here; the guard keeps a future caller from crashing on a
+    // stray id.
+    if (record === undefined) return
+    void record.session.open()
+  }
+
   /**
    * Open a healthy catalog child through its direct-parent address.
    * @param address - catalog-derived parent and child ids.
diff --git a/packages/client/runtime/src/client/slots.ts b/packages/client/runtime/src/client/slots.ts
index c5680af..2172c3d 100644
--- a/packages/client/runtime/src/client/slots.ts
+++ b/packages/client/runtime/src/client/slots.ts
@@ -16,6 +16,7 @@
  * redundancy. */
 import { Service } from '@deepseek-ai/cordis'
 import type { Context } from '@deepseek-ai/cordis'
+import type { SessionId } from '@deepseek-ai/dsh-api-remotes/client'
 import { SlotCore } from '@deepseek-ai/dsh-client-ui-slots'
 import type {
   LiveSlotNode, LocaleFace, OwnerOf, SlotEntryDef, SlotMap, SlotRenderer, SlotRendererHost,
@@ -412,6 +413,7 @@ export class SlotRegistry extends Service {
       sessions: {
         list: sessions.list,
         provideInfo: sessions.currentProvideInfo,
+        infoFor: id => sessions.infoFor(id as SessionId),
       },
       workspaces: { list: workspaces.list },
       get locale() { return service._locale },
diff --git a/packages/client/ui-layout/src/client/AppFrame.module.css b/packages/client/ui-layout/src/client/AppFrame.module.css
index ae56780..d0b5c57 100644
--- a/packages/client/ui-layout/src/client/AppFrame.module.css
+++ b/packages/client/ui-layout/src/client/AppFrame.module.css
@@ -117,3 +117,146 @@
 .overlayLayer > * {
   pointer-events: auto;
 }
+
+/* Multi-window grid: the center column's body is a column that hosts a
+   toolbar over a responsive pane grid (up to GRID_SIZE live chats). */
+.grid {
+  min-height: 0;
+  height: 100%;
+  display: flex;
+  flex-direction: column;
+  overflow: hidden;
+}
+
+.gridToolbar {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 8px;
+  padding: 6px 12px;
+  border-bottom: 1px solid var(--dsw-alias-border-l1);
+  flex: none;
+}
+
+.gridLabel {
+  font-size: 12px;
+  color: var(--dsw-alias-text-secondary);
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+}
+
+.gridControls {
+  display: flex;
+  align-items: center;
+  gap: 6px;
+  flex: none;
+}
+
+.gridArrow {
+  min-width: 24px;
+  height: 24px;
+  border: 1px solid var(--dsw-alias-border-l2);
+  border-radius: 6px;
+  background: var(--dsw-alias-button-floating-fill);
+  color: var(--dsw-alias-text-primary);
+  cursor: pointer;
+  line-height: 1;
+}
+
+.gridArrow:disabled {
+  opacity: 0.4;
+  cursor: default;
+}
+
+.gridPage {
+  font-size: 12px;
+  color: var(--dsw-alias-text-secondary);
+  min-width: 16px;
+  text-align: center;
+}
+
+.gridAdd {
+  height: 24px;
+  border: 1px solid var(--dsw-alias-border-l2);
+  border-radius: 6px;
+  background: var(--dsw-alias-button-floating-fill);
+  color: var(--dsw-alias-text-primary);
+  font-size: 12px;
+  max-width: 180px;
+}
+
+.gridBody {
+  flex: 1;
+  min-height: 0;
+  display: grid;
+  gap: 1px;
+  padding: 1px;
+  background: var(--dsw-alias-border-l1);
+  overflow: hidden;
+}
+
+.gridBody[data-count='1'] { grid-template-columns: 1fr; grid-template-rows: 1fr; }
+.gridBody[data-count='2'] { grid-template-columns: 1fr 1fr; grid-template-rows: 1fr; }
+.gridBody[data-count='3'] { grid-template-columns: 1fr 1fr; grid-template-rows: 1fr 1fr; }
+.gridBody[data-count='4'] { grid-template-columns: 1fr 1fr; grid-template-rows: 1fr 1fr; }
+.gridBody[data-count='5'],
+.gridBody[data-count='6'] { grid-template-columns: repeat(3, 1fr); grid-template-rows: repeat(2, 1fr); }
+
+.gridEmpty {
+  flex: 1;
+  min-height: 0;
+  overflow: hidden;
+}
+
+.pane {
+  min-width: 0;
+  min-height: 0;
+  display: flex;
+  flex-direction: column;
+  background: var(--dsw-alias-bg-base);
+  overflow: hidden;
+}
+
+.paneHeader {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 8px;
+  padding: 4px 8px;
+  border-bottom: 1px solid var(--dsw-alias-border-l1);
+  background: var(--dsw-specific-sidebar-fill);
+  flex: none;
+}
+
+.paneTitle {
+  font-size: 12px;
+  font-weight: 600;
+  color: var(--dsw-alias-text-primary);
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+}
+
+.paneClose {
+  flex: none;
+  width: 20px;
+  height: 20px;
+  border: none;
+  border-radius: 5px;
+  background: transparent;
+  color: var(--dsw-alias-text-secondary);
+  cursor: pointer;
+  line-height: 1;
+}
+
+.paneClose:hover {
+  background: var(--dsw-alias-button-floating-hover);
+  color: var(--dsw-alias-text-primary);
+}
+
+.paneBody {
+  flex: 1;
+  min-height: 0;
+  overflow: hidden;
+}
diff --git a/packages/client/ui-layout/src/client/AppFrame.tsx b/packages/client/ui-layout/src/client/AppFrame.tsx
index 2696dc9..afcb2d1 100644
--- a/packages/client/ui-layout/src/client/AppFrame.tsx
+++ b/packages/client/ui-layout/src/client/AppFrame.tsx
@@ -12,16 +12,20 @@
  */
 import { useCallback, useEffect, useLayoutEffect, useRef, useState } from 'react'
 import type { ReactNode } from 'react'
-import type { PropsRenderSlots, PropsRuntime, PropsStore } from '@deepseek-ai/dsh-client-ui-slots'
+import type {
+  PropsRenderSlots, PropsRuntime, PropsStore, SessionPinnedProviderComponent, SnapshotSelectorHook,
+} from '@deepseek-ai/dsh-client-ui-slots'
+import type { SessionId, SessionListState } from '@deepseek-ai/dsh-client-runtime/client'
 import { computeColumns, SIDEBAR_AUTO_COLLAPSE, SIDEBAR_DEFAULT } from './columns.ts'
-import type { createLayoutStore } from './stores.ts'
+import { GRID_SIZE, type createLayoutStore } from './stores.ts'
 import css from './AppFrame.module.css'
 
-/** Full composed props: runtime share + child-slot render share + store share. */
+/** Full composed props: runtime share + child-slot render share + store share + inject face. */
 export type AppFrameProps =
   & PropsRuntime<'root'>
   & PropsRenderSlots<'sidebar' | 'conversation' | 'details' | 'shell.overlay'>
   & PropsStore<ReturnType<typeof createLayoutStore>>
+  & { pinOpen: (id: SessionId) => void }
 
 /** Center column grid item (session-body building block). */
 function CenterColumn(props: { children?: ReactNode }) {
@@ -83,12 +87,96 @@ function DragHandle(props: { side: 'sidebar' | 'details'; left: number; onStart:
   )
 }
 
+/**
+ * Multi-window grid: a toolbar (add a session, slide pages) plus a responsive
+ * grid of up to {@link GRID_SIZE} live chat panes, each pinned to its own
+ * session via the framework's SessionPinnedProvider seat. With no pinned
+ * sessions it falls back to the ordinary single conversation slot so the
+ * frame stays usable before the user opens any window.
+ */
+function MultiPaneGrid({
+  pinned, page, useSessions, actions, renderSlot, SessionPinnedProvider, pinOpen,
+}: {
+  pinned: SessionId[]
+  page: number
+  useSessions: SnapshotSelectorHook<SessionListState>
+  actions: { addSession(id: SessionId): void; removeSession(id: SessionId): void; setPage(page: number): void }
+  renderSlot: PropsRenderSlots<'sidebar' | 'conversation' | 'details' | 'shell.overlay'>['renderSlot']
+  SessionPinnedProvider: SessionPinnedProviderComponent
+  pinOpen: (id: SessionId) => void
+}) {
+  const byId = useSessions(s => s.byId)
+  const live = pinned.filter(id => byId[id] !== undefined)
+  // Open each pinned window's history once pinned; Session.open() /
+  // SessionRuntime.pinOpen() are idempotent and no-op for unresolvable ids,
+  // and `pinned` is a reference-stable store snapshot, so the effect scopes to
+  // pin changes.
+  useEffect(() => {
+    for (const id of pinned) pinOpen(id)
+  }, [pinned, pinOpen])
+  const start = page * GRID_SIZE
+  const windowIds = live.slice(start, start + GRID_SIZE)
+  const unpinned = Object.values(byId).filter(s => !pinned.includes(s.id))
+  const hasMore = live.length > GRID_SIZE
+
+  return (
+    <div className={css.grid}>
+      <div className={css.gridToolbar}>
+        <span className={css.gridLabel}>
+          {live.length === 0 ? 'Chat windows' : `${live.length} chat window${live.length === 1 ? '' : 's'}`}
+        </span>
+        <div className={css.gridControls}>
+          {hasMore && (
+            <>
+              <button type="button" className={css.gridArrow} onClick={() => actions.setPage(page - 1)} disabled={page === 0} aria-label="Previous page">‹</button>
+              <span className={css.gridPage}>{page + 1}</span>
+              <button type="button" className={css.gridArrow} onClick={() => actions.setPage(page + 1)} disabled={start + GRID_SIZE >= live.length} aria-label="Next page">›</button>
+            </>
+          )}
+          <select
+            className={css.gridAdd}
+            value=""
+            aria-label="Add chat window"
+            onChange={(e) => {
+              if (e.target.value !== '') actions.addSession(e.target.value as SessionId)
+            }}
+          >
+            <option value="">＋ Add window…</option>
+            {unpinned.map(s => <option key={s.id} value={s.id}>{s.displayTitle}</option>)}
+          </select>
+        </div>
+      </div>
+      {windowIds.length === 0 ? (
+        <div className={css.gridEmpty}>{renderSlot('conversation', {})}</div>
+      ) : (
+        <div className={css.gridBody} data-count={windowIds.length}>
+          {windowIds.map(id => (
+            <section className={css.pane} key={id}>
+              <header className={css.paneHeader}>
+                <span className={css.paneTitle} title={id}>{byId[id]?.displayTitle ?? id}</span>
+                <button type="button" className={css.paneClose} onClick={() => actions.removeSession(id)} aria-label={`Close ${byId[id]?.displayTitle ?? id}`}>×</button>
+              </header>
+              <div className={css.paneBody}>
+                <SessionPinnedProvider sessionId={id}>
+                  {renderSlot('conversation', {})}
+                </SessionPinnedProvider>
+              </div>
+            </section>
+          ))}
+        </div>
+      )}
+    </div>
+  )
+}
+
 /** The three-column frame (see module doc). */
 export function AppFrame({
   useStore,
   useSessions,
   actions,
   renderSlot,
+  SessionPinnedProvider,
+  pinOpen,
 }: AppFrameProps) {
   const panels = useStore(s => s)
   const detailsSession = useSessions((s) => {
@@ -186,8 +274,24 @@ export function AppFrame({
             paint — no loading gate: a bare status line reads worse than
             the shell's own pending rendering. The conversation
             is session-maybe; the strict details entry naturally renders
-            empty while no session is current. */}
-        <CenterColumn>{renderSlot('conversation', {})}</CenterColumn>
+            empty while no session is current. The center hosts the
+            multi-window grid once the user pins sessions; otherwise it
+            falls back to the ordinary single conversation. */}
+        <CenterColumn>
+          {SessionPinnedProvider === undefined
+            ? renderSlot('conversation', {})
+            : (
+            <MultiPaneGrid
+              pinned={panels.pinned}
+              page={panels.page}
+              useSessions={useSessions}
+              actions={actions}
+              renderSlot={renderSlot}
+              SessionPinnedProvider={SessionPinnedProvider}
+              pinOpen={pinOpen}
+            />
+            )}
+        </CenterColumn>
         <DetailsColumn>{renderSlot('details', {})}</DetailsColumn>
       </>
       <div className={css.overlayLayer} data-shell-overlay>
diff --git a/packages/client/ui-layout/src/client/index.ts b/packages/client/ui-layout/src/client/index.ts
index c56d83b..2024fe9 100644
--- a/packages/client/ui-layout/src/client/index.ts
+++ b/packages/client/ui-layout/src/client/index.ts
@@ -7,7 +7,7 @@
  * with the runtime sessions service. A second effect seats the theme
  * presenter, which projects ctx.theme snapshots onto document.body.
  */
-import type { ClientContext } from '@deepseek-ai/dsh-client-runtime/client'
+import type { ClientContext, SessionId } from '@deepseek-ai/dsh-client-runtime/client'
 import type {} from '@deepseek-ai/dsh-client-ui-theme/client'
 import type { PanelActions } from './service.ts'
 import { AppFrame } from './AppFrame.tsx'
@@ -132,7 +132,9 @@ export function apply(ctx: ClientContext): void {
       // conversation business actions belong to their registrants.
       inject: (actions: PanelActions) => {
         layout.attachPanels(actions)
-        return {}
+        // Multi-pane feed: opening a pinned window's history without changing
+        // the current selection (see AppFrame's pinOpen effect).
+        return { pinOpen: (id: SessionId) => ctx.sessions.pinOpen(id) }
       },
     }, AppFrame)
     return () => {
diff --git a/packages/client/ui-layout/src/client/stores.ts b/packages/client/ui-layout/src/client/stores.ts
index d2de668..44eef49 100644
--- a/packages/client/ui-layout/src/client/stores.ts
+++ b/packages/client/ui-layout/src/client/stores.ts
@@ -8,19 +8,38 @@
  * receives the bound actions through the registration's inject hook.
  */
 import { defineStore, type EngineStoreHandle } from '@deepseek-ai/dsh-client-runtime/client'
+import type { SessionId } from '@deepseek-ai/dsh-client-runtime/client'
 import {
   clampWidth, DETAILS_DEFAULT, DETAILS_MAX, DETAILS_MIN,
   SIDEBAR_DEFAULT, SIDEBAR_MAX, SIDEBAR_MIN,
 } from './columns.ts'
 
+/** Max concurrent chat panes in the multi-window grid (a slider pages beyond it). */
+export const GRID_SIZE = 6
+
+/** Clamp a slider page to the last full window of a list of the given length. */
+function clampPage(page: number, length: number): number {
+  const maxPage = Math.max(0, Math.ceil(length / GRID_SIZE) - 1)
+  return Math.min(maxPage, Math.max(0, page))
+}
+
 /**
  * Layout store state: panel width preferences in px (0 = closed), plus the
  * narrow-viewport pair — `narrow` mirrors AppFrame's breakpoint reading
  * (viewport < SIDEBAR_AUTO_COLLAPSE) so toggleSidebar can pick semantics, and
  * `narrowExpanded` is the manual override that re-expands the auto-collapsed
  * sidebar over the squeezed center without rewriting the width preference.
+ * The multi-window seat keeps the ordered pinned session ids (`pinned`) and
+ * the current slider page (`page`).
  */
-type LayoutState = { sidebar: number; details: number; narrow: boolean; narrowExpanded: boolean }
+type LayoutState = {
+  sidebar: number
+  details: number
+  narrow: boolean
+  narrowExpanded: boolean
+  pinned: SessionId[]
+  page: number
+}
 
 /**
  * Annotation twin of the actions literal below (the export needs a declared
@@ -33,6 +52,10 @@ type LayoutActions = {
   setNarrow: (draft: LayoutState, narrow: boolean) => void
   openDetails: (draft: LayoutState) => void
   closeDetails: (draft: LayoutState) => void
+  addSession: (draft: LayoutState, id: SessionId) => void
+  removeSession: (draft: LayoutState, id: SessionId) => void
+  setPage: (draft: LayoutState, page: number) => void
+  resetPinned: (draft: LayoutState, ids: SessionId[]) => void
 }
 
 /**
@@ -47,7 +70,10 @@ type LayoutActions = {
  */
 export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutActions>  {
   const handle = defineStore({
-    init: (): LayoutState => ({ sidebar: SIDEBAR_DEFAULT, details: 0, narrow: false, narrowExpanded: false }),
+    init: (): LayoutState => ({
+      sidebar: SIDEBAR_DEFAULT, details: 0, narrow: false, narrowExpanded: false,
+      pinned: [], page: 0,
+    }),
     actions: {
       setSidebar: (d, px: number) => { d.sidebar = clampWidth(px, SIDEBAR_MIN, SIDEBAR_MAX) },
       setDetails: (d, px: number) => { d.details = clampWidth(px, DETAILS_MIN, DETAILS_MAX) },
@@ -66,6 +92,20 @@ export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutAction
       },
       openDetails: (d) => { if (d.details === 0) d.details = DETAILS_DEFAULT },
       closeDetails: (d) => { d.details = 0 },
+      addSession: (d, id: SessionId) => {
+        if (d.pinned.includes(id)) return
+        d.pinned.push(id)
+        d.page = clampPage(d.page, d.pinned.length)
+      },
+      removeSession: (d, id: SessionId) => {
+        d.pinned = d.pinned.filter(p => p !== id)
+        d.page = clampPage(d.page, d.pinned.length)
+      },
+      setPage: (d, page: number) => { d.page = clampPage(page, d.pinned.length) },
+      resetPinned: (d, ids: SessionId[]) => {
+        d.pinned = [...ids]
+        d.page = 0
+      },
     },
   })
   return handle
diff --git a/packages/client/ui-layout/tests/app-frame.client.spec.tsx b/packages/client/ui-layout/tests/app-frame.client.spec.tsx
index 65de5a6..736fe5b 100644
--- a/packages/client/ui-layout/tests/app-frame.client.spec.tsx
+++ b/packages/client/ui-layout/tests/app-frame.client.spec.tsx
@@ -88,6 +88,7 @@ function mountFrame() {
       useSessions={useSessions}
       useWorkspaces={((sel: (s: WorkspaceListState) => unknown) => sel(workspaceState)) as never}
       SessionProvider={SessionProviderStub}
+      pinOpen={(_id) => { /* multi-pane feed: no session history in this bare render bench */ }}
     />
   )
   const utils = render(element())
diff --git a/packages/client/ui-layout/tests/apply.client.spec.ts b/packages/client/ui-layout/tests/apply.client.spec.ts
index 95f8cad..8c6b57d 100644
--- a/packages/client/ui-layout/tests/apply.client.spec.ts
+++ b/packages/client/ui-layout/tests/apply.client.spec.ts
@@ -10,6 +10,7 @@ import { Context } from '@deepseek-ai/cordis'
 import { stubSettingsScope } from '@deepseek-ai/dsh-client-test-runtime'
 import { beforeEach, describe, expect, it, vi } from 'vitest'
 import { SlotRegistry } from '@deepseek-ai/dsh-client-runtime/client'
+import type { SessionId } from '@deepseek-ai/dsh-client-runtime/client'
 import { LocaleRuntime } from '@deepseek-ai/dsh-client-locale/client'
 import { apply as themeApply, inject as themeInject, ThemeRuntime } from '@deepseek-ai/dsh-client-ui-theme/client'
 import { apply, inject, LayoutController } from '@deepseek-ai/dsh-client-ui-layout/client'
@@ -60,8 +61,12 @@ describe('ui-layout client apply', () => {
     const actions = {
       setSidebar: vi.fn(), setDetails: vi.fn(), toggleSidebar: vi.fn(), openDetails: vi.fn(), closeDetails: vi.fn(),
     }
-    const injected = (slots.entries('root')[0]!.inject as (actions: never) => object)(actions as never)
-    expect(injected).toEqual({})
+    const injected = (slots.entries('root')[0]!.inject as (
+      actions: never,
+    ) => { pinOpen: (id: SessionId) => void })(actions as never)
+    // No business face: the only injected member is the multi-pane pinOpen callback.
+    expect(Object.keys(injected)).toEqual(['pinOpen'])
+    expect(typeof injected.pinOpen).toBe('function')
     const layout = ctx.get('layout') as LayoutController
     layout.toggleSidebar()
     expect(actions.toggleSidebar).toHaveBeenCalledOnce()
diff --git a/packages/client/ui-layout/tests/layout-store.client.spec.ts b/packages/client/ui-layout/tests/layout-store.client.spec.ts
index c6f0069..5bb081e 100644
--- a/packages/client/ui-layout/tests/layout-store.client.spec.ts
+++ b/packages/client/ui-layout/tests/layout-store.client.spec.ts
@@ -6,7 +6,8 @@
  * real engine instance (same create path as production).
  */
 import { beforeEach, describe, expect, it } from 'vitest'
-import { createLayoutStore } from '@deepseek-ai/dsh-client-ui-layout/src/client/stores.ts'
+import type { SessionId } from '@deepseek-ai/dsh-client-runtime/client'
+import { createLayoutStore, GRID_SIZE } from '@deepseek-ai/dsh-client-ui-layout/src/client/stores.ts'
 import {
   DETAILS_DEFAULT, DETAILS_MAX, DETAILS_MIN,
   SIDEBAR_DEFAULT, SIDEBAR_MAX, SIDEBAR_MIN,
@@ -19,7 +20,7 @@ beforeEach(() => { localStorage.clear() })
 describe('createLayoutStore', () => {
   it('initializes the sidebar at its default width, details closed, wide viewport assumed', () => {
     const { store } = createLayoutStore().create()
-    expect(store.getSnapshot()).toEqual({ sidebar: SIDEBAR_DEFAULT, details: 0, narrow: false, narrowExpanded: false })
+    expect(store.getSnapshot()).toEqual({ sidebar: SIDEBAR_DEFAULT, details: 0, narrow: false, narrowExpanded: false, pinned: [], page: 0 })
   })
 
   it('each create() is an independent instance (factory is not a singleton)', () => {
@@ -55,7 +56,7 @@ describe('createLayoutStore', () => {
     actions.setSidebar(400)
     actions.setNarrow(true)
     actions.toggleSidebar()
-    expect(store.getSnapshot()).toEqual({ sidebar: 400, details: 0, narrow: true, narrowExpanded: true })
+    expect(store.getSnapshot()).toEqual({ sidebar: 400, details: 0, narrow: true, narrowExpanded: true, pinned: [], page: 0 })
     actions.toggleSidebar()
     expect(store.getSnapshot().narrowExpanded).toBe(false)
     expect(store.getSnapshot().sidebar).toBe(400)
@@ -98,6 +99,32 @@ describe('createLayoutStore', () => {
       details: 0,
       narrow: false,
       narrowExpanded: false,
+      pinned: [],
+      page: 0,
     })
   })
+
+  it('pins and unpins sessions, dedupes, and slides pages within the window bound', () => {
+    const { store, actions } = createLayoutStore().create()
+    const s = (id: string) => id as SessionId
+    actions.addSession(s('a'))
+    actions.addSession(s('a'))
+    actions.addSession(s('b'))
+    actions.addSession(s('c'))
+    expect(store.getSnapshot().pinned).toEqual([s('a'), s('b'), s('c')])
+    expect(store.getSnapshot().page).toBe(0)
+
+    // Adding beyond GRID_SIZE enables the slider; page clamps to the last window.
+    for (let i = 0; i < GRID_SIZE; i++) actions.addSession(s(`s${i}`))
+    actions.setPage(99)
+    expect(store.getSnapshot().page).toBe(1)
+
+    actions.setPage(0)
+    actions.removeSession(s('b'))
+    expect(store.getSnapshot().pinned).toEqual([s('a'), s('c'), s('s0'), s('s1'), s('s2'), s('s3'), s('s4'), s('s5')])
+
+    actions.resetPinned([s('x'), s('y')])
+    expect(store.getSnapshot().pinned).toEqual([s('x'), s('y')])
+    expect(store.getSnapshot().page).toBe(0)
+  })
 })
diff --git a/packages/client/ui-layout/tests/service.client.spec.ts b/packages/client/ui-layout/tests/service.client.spec.ts
index e1267b4..2b313f5 100644
--- a/packages/client/ui-layout/tests/service.client.spec.ts
+++ b/packages/client/ui-layout/tests/service.client.spec.ts
@@ -16,6 +16,10 @@ function fakePanels(): PanelActions {
     setNarrow: vi.fn(),
     openDetails: vi.fn(),
     closeDetails: vi.fn(),
+    addSession: vi.fn(),
+    removeSession: vi.fn(),
+    setPage: vi.fn(),
+    resetPinned: vi.fn(),
   }
 }
 
diff --git a/packages/client/ui-slots/src/index.ts b/packages/client/ui-slots/src/index.ts
index 00e2eaa..80d7b0e 100644
--- a/packages/client/ui-slots/src/index.ts
+++ b/packages/client/ui-slots/src/index.ts
@@ -324,6 +324,21 @@ export interface SessionAreaProps {
  */
 export type SessionProviderComponent = (props: SessionAreaProps) => ReactNode
 
+/** Props of the standard-kit pinned-session provider seat (multi-pane). */
+export interface SessionPinnedAreaProps {
+  /** The session id to pin (listed sessions render; unknown ids render nothing). */
+  sessionId: string
+  /** Session body rendered under the pinned session's scope. */
+  children: ReactNode
+}
+
+/**
+ * Framework-wired pinned-session provider component. It resolves one specific
+ * session's provide bundle and renders the body scoped to it; business code
+ * does not import it directly.
+ */
+export type SessionPinnedProviderComponent = (props: SessionPinnedAreaProps) => ReactNode
+
 /**
  * Child-slot render share: `renderSlot` statically narrowed to the entry's
  * declared children keys. Delegation is plain props passing (hand
@@ -360,7 +375,14 @@ export type PropsRenderSlots<S extends keyof SlotMap & string> = {
   // a session-scope child is what makes a session area exist, so the seat
   // derives from the children key set's scopes (renderer injects the value).
   ? { SessionProvider: SessionProviderComponent }
-  : object)
+  : object) & {
+    // The pinned-session seat is unconditional: any entry that declares
+    // children may render a session subtree pinned to a specific session id
+    // (the multi-pane grid), so the framework hands every such entry the seat.
+    // Optional in the type so hand-built props (tests) may omit it; the
+    // renderer always supplies it for entries that declare children.
+    SessionPinnedProvider?: SessionPinnedProviderComponent
+  }
 
 /**
  * Registration-position component shape: the bare call signature, so composed
diff --git a/packages/client/ui-slots/src/renderer.ts b/packages/client/ui-slots/src/renderer.ts
index cdad419..6787e07 100644
--- a/packages/client/ui-slots/src/renderer.ts
+++ b/packages/client/ui-slots/src/renderer.ts
@@ -171,6 +171,12 @@ export interface SlotRendererHost {
      * undefined while no current session resolves.
      */
     provideInfo: HostObservable<SessionMaybeProvideInfo>
+    /**
+     * Multi-pane seat: observable resolve of one specific session's provide
+     * bundle, keyed by an explicit session id rather than the current
+     * selection. Snapshots `undefined` while the session cannot resolve.
+     */
+    infoFor(id: string): HostObservable<SessionProvideInfo | undefined>
   }
   /** Workspace-side standard-kit sources. */
   workspaces: {
diff --git a/packages/client/web-react/src/index.ts b/packages/client/web-react/src/index.ts
index 5debff6..9cfdec1 100644
--- a/packages/client/web-react/src/index.ts
+++ b/packages/client/web-react/src/index.ts
@@ -18,6 +18,6 @@ export type {
 export { SlotOwnershipError, StaleAuthorizationError } from '@deepseek-ai/dsh-client-ui-slots'
 export { createSlotRenderer } from './scoped-slots.tsx'
 
-export { SessionProvider, SlotAssemblyError, type SessionProviderProps } from './session-provider.tsx'
+export { SessionProvider, SessionPinnedProvider, SlotAssemblyError, type SessionProviderProps, type SessionPinnedProviderProps } from './session-provider.tsx'
 
 export { useInvoke } from './use-invoke.ts'
diff --git a/packages/client/web-react/src/scoped-slots.tsx b/packages/client/web-react/src/scoped-slots.tsx
index dea0511..1f52b86 100644
--- a/packages/client/web-react/src/scoped-slots.tsx
+++ b/packages/client/web-react/src/scoped-slots.tsx
@@ -10,8 +10,9 @@ import {
   type SlotScope, type StoredEntry, type Translate,
 } from '@deepseek-ai/dsh-client-ui-slots'
 import {
-  HostContext, SessionMaybeProvider, SessionProvider, SlotAssemblyError, maybeObservableHook,
-  observableHook, projectionHook, useHost, useSessionMaybeProvideInfo,
+  HostContext, SessionMaybeProvider, SessionPinnedProvider, SessionProvider,
+  SlotAssemblyError, maybeObservableHook, observableHook, projectionHook, useHost,
+  useSessionMaybeProvideInfo,
 } from './session-provider.tsx'
 
 type InjectedProps = Record<string, unknown>
@@ -436,6 +437,9 @@ function standardKit(
     if (Object.values(entry.children).some(spec => spec.scope === 'session')) {
       kit['SessionProvider'] = SessionProvider
     }
+    // Pinned-session seat: every children-declaring entry can render a
+    // session subtree bound to an explicit session id (the multi-pane grid).
+    kit['SessionPinnedProvider'] = SessionPinnedProvider
   }
   return { kit, standard, actions: store?.actions }
 }
diff --git a/packages/client/web-react/src/session-provider.tsx b/packages/client/web-react/src/session-provider.tsx
index 196a4d9..d476a96 100644
--- a/packages/client/web-react/src/session-provider.tsx
+++ b/packages/client/web-react/src/session-provider.tsx
@@ -141,15 +141,17 @@ export interface SessionProviderProps {
 }
 
 /**
- * Framework-wired session area: subscribes to the host's current provide
- * source and remounts the body under `key={sessionId}` so a session switch
- * rebuilds the session subtree. This dependency-inverted layer uses plain
- * string ids; `PropsRuntime` applies the branded type at the component
- * boundary.
+ * Framework-wired session area: reads the nearest binding context and
+ * remounts the body under `key={sessionId}` so the session body rebuilds
+ * whenever the resolved session changes. In the ordinary single-session GUI
+ * the nearest context is the root `SessionMaybeProvider` (current selection);
+ * inside a pinned pane it is the `SessionPinnedProvider` for that pane, so
+ * nested session-scoped slots follow the pinned session rather than the
+ * global current. This dependency-inverted layer uses plain string ids;
+ * `PropsRuntime` applies the branded type at the component boundary.
  */
 export function SessionProvider({ empty, children }: SessionProviderProps) {
-  const host = useHost()
-  const info = observableHook(host.sessions.provideInfo)(s => s)
+  const info = useSessionMaybeProvideInfo()
   const id = info.sessionId
   if (id === undefined) return <>{empty?.() ?? null}</>
   return (
@@ -158,3 +160,29 @@ export function SessionProvider({ empty, children }: SessionProviderProps) {
     </BindingContext.Provider>
   )
 }
+
+/** Pinned-session provider API: an explicit session id and a body. */
+export interface SessionPinnedProviderProps {
+  /** The session id to pin (listed sessions render; unknown ids render nothing). */
+  sessionId: string
+  /** Session body rendered under the pinned session's scope. */
+  children: ReactNode
+}
+
+/**
+ * Pinned-session area: resolves one specific session's provide bundle (the
+ * multi-pane seat, backed by the runtime's `sessions.infoFor`) and provides
+ * it as the nearest binding context, so the subtree renders as a full
+ * conversation for that session regardless of the global current selection.
+ * Unknown/unlisted sessions render nothing.
+ */
+export function SessionPinnedProvider({ sessionId, children }: SessionPinnedProviderProps) {
+  const host = useHost()
+  const info = observableHook(host.sessions.infoFor(sessionId))(s => s)
+  if (info?.sessionId === undefined) return null
+  return (
+    <BindingContext.Provider value={info} key={sessionId}>
+      {children}
+    </BindingContext.Provider>
+  )
+}
diff --git a/packages/client/web-react/tests/scoped-slots-real-core.client.spec.tsx b/packages/client/web-react/tests/scoped-slots-real-core.client.spec.tsx
index 0e94030..70f201a 100644
--- a/packages/client/web-react/tests/scoped-slots-real-core.client.spec.tsx
+++ b/packages/client/web-react/tests/scoped-slots-real-core.client.spec.tsx
@@ -39,6 +39,7 @@ function hostOver(core: SlotCore): SlotRendererHost {
     sessions: {
       list: { getSnapshot: () => ({}), subscribe: () => () => {} },
       provideInfo: { getSnapshot: () => absentInfo, subscribe: () => () => {} },
+      infoFor: () => ({ getSnapshot: () => undefined, subscribe: () => () => {} }),
     },
     workspaces: {
       list: { getSnapshot: () => ({}), subscribe: () => () => {} },
diff --git a/packages/client/web-react/tests/scoped-slots.client.spec.tsx b/packages/client/web-react/tests/scoped-slots.client.spec.tsx
index b5d7e2d..b6c177a 100644
--- a/packages/client/web-react/tests/scoped-slots.client.spec.tsx
+++ b/packages/client/web-react/tests/scoped-slots.client.spec.tsx
@@ -151,6 +151,7 @@ function makeHost() {
     sessions: {
       list,
       provideInfo: provide,
+      infoFor: () => ({ getSnapshot: () => undefined, subscribe: () => () => {} }),
     },
     workspaces: { list: workspaces },
   }
diff --git a/packages/client/web-react/tests/session-provider.client.spec.tsx b/packages/client/web-react/tests/session-provider.client.spec.tsx
index af46f41..5e2c2d8 100644
--- a/packages/client/web-react/tests/session-provider.client.spec.tsx
+++ b/packages/client/web-react/tests/session-provider.client.spec.tsx
@@ -56,6 +56,7 @@ function makeHost(bodies: { root: (rp: (key: string, owner: object) => React.Rea
     sessions: {
       list: observable<unknown>({ ids: [] }),
       provideInfo: provide,
+      infoFor: () => ({ getSnapshot: () => undefined, subscribe: () => () => {} }),
     },
     workspaces: { list: observable<unknown>({ items: [] }) },
   }
@@ -187,11 +188,11 @@ describe('SessionProvider', () => {
     expect(seen.at(-1)).toBe('now-live')
   })
 
-  it('fails loud when mounted outside the renderer tree (no host channel)', () => {
+  it('fails loud when mounted outside the renderer tree (no binding provider)', () => {
     const spy = vi.spyOn(console, 'error').mockImplementation(() => {})
     expect(() => render(
       <SessionProvider>{id => <b>{id}</b>}</SessionProvider>,
-    )).toThrow(/outside the installed renderer tree/)
+    )).toThrow(/outside the root binding provider/)
     spy.mockRestore()
   })
 })
diff --git a/packages/client/web-react/tests/stale-authorization.client.spec.tsx b/packages/client/web-react/tests/stale-authorization.client.spec.tsx
index e617dde..9c0e791 100644
--- a/packages/client/web-react/tests/stale-authorization.client.spec.tsx
+++ b/packages/client/web-react/tests/stale-authorization.client.spec.tsx
@@ -45,6 +45,7 @@ function makeHost() {
     sessions: {
       list: { getSnapshot: () => ({}), subscribe: () => () => {} },
       provideInfo: { getSnapshot: () => absentInfo, subscribe: () => () => {} },
+      infoFor: () => ({ getSnapshot: () => undefined, subscribe: () => () => {} }),
     },
     workspaces: {
       list: { getSnapshot: () => ({}), subscribe: () => () => {} },
diff --git a/packages/client/web-react/tests/use-projection.client.spec.tsx b/packages/client/web-react/tests/use-projection.client.spec.tsx
index 1af4f1a..dd11ea1 100644
--- a/packages/client/web-react/tests/use-projection.client.spec.tsx
+++ b/packages/client/web-react/tests/use-projection.client.spec.tsx
@@ -59,6 +59,7 @@ function makeHost() {
     sessions: {
       list: observable<unknown>({ ids: [] }),
       provideInfo: provide,
+      infoFor: () => ({ getSnapshot: () => undefined, subscribe: () => () => {} }),
     },
     workspaces: { list: observable<unknown>({ items: [] }) },
   }
diff --git a/packages/test-support/client-runtime/src/sessions.ts b/packages/test-support/client-runtime/src/sessions.ts
index c014d6f..0425941 100644
--- a/packages/test-support/client-runtime/src/sessions.ts
+++ b/packages/test-support/client-runtime/src/sessions.ts
@@ -185,7 +185,7 @@ export class TestSessions implements ISessions {
   /** Calls observed on the service-level face, newest last. */
   readonly calls: {
     method: 'open' | 'openSubagent' | 'setSubagentCatalogOpen' | 'refreshSubagents'
-      | 'clear' | 'search' | 'fork'
+      | 'clear' | 'search' | 'fork' | 'pinOpen'
     args: unknown[]
   }[] = []
 
@@ -348,6 +348,22 @@ export class TestSessions implements ISessions {
     return (id === undefined ? undefined : this.provideInfo(id)) ?? this.channel.maybeInfo
   }
 
+  /**
+   * Multi-pane seat (production parity): observable resolve of one session's
+   * standard-props bundle. The test double resolves lazily and re-snapshots on
+   * every read; the bench subscribes to the list so a fixture appearing later
+   * surfaces.
+   * @param id - session id.
+   * @returns an observable of the bundle, `undefined` while the session cannot resolve.
+   */
+  infoFor(id: SessionId): HostObservable<SessionProvideInfo | undefined> {
+    const self = this
+    return {
+      getSnapshot: () => self.provideInfo(id),
+      subscribe: (fn) => self.list.subscribe(() => fn()),
+    }
+  }
+
   /**
    * Resolve (mint on first touch) the session-scoped Cordis context through
    * the production `createScope`, so real `scopeOf`/scope-addressed services
@@ -413,6 +429,12 @@ export class TestSessions implements ISessions {
     })
   }
 
+  /** Open a pinned session's window (idempotent; does not change selection). */
+  pinOpen(id: SessionId): void {
+    this.calls.push({ method: 'pinOpen', args: [id] })
+    this.require(id)
+  }
+
   /** Open an existing fixture through its catalog address. */
   openSubagent(address: SubagentAddress): void {
     this.calls.push({ method: 'openSubagent', args: [address] })
